# Politica sulla Sicurezza (Security Policy)

Questo repository funge da **Guida e Template** per l'uso dello strumento `git-crypt`. Non contiene segreti di produzione attivi.

## ⚠️ Segnalazione di Vulnerabilità

Se noti una potenziale vulnerabilità in questa guida (ad esempio, un file d'esempio che dovrebbe essere criptato ma non lo è, a causa di un pattern `.gitattributes` errato), ti preghiamo di:

1.  **Aprire immediatamente una "Issue" (Problema) su GitHub** descrivendo chiaramente l'errore di configurazione.
2.  **NON includere dati sensibili reali** nella segnalazione.

## 🔗 Riferimenti a Git-crypt Ufficiale

Per problemi di sicurezza che riguardano lo **strumento `git-crypt`** stesso (il codice binario), si prega di fare riferimento alla politica di sicurezza del repository ufficiale:

*   **Git-crypt Repository Ufficiale:** [https://github.com/AGWA/git-crypt](https://github.com/AGWA/git-crypt)
